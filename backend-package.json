{
  "name": "mioplan-timeline-backend",
  "version": "1.0.0",
  "description": "Backend for MioPlan Timeline",
  "main": "server/server.js",
  "scripts": {
    "start": "cd server && npm install && npm start",
    "build": "cd server && npm install"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
